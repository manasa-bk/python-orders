apiVersion: v1
kind: Service
metadata:
  name: {{ include "python_practice.fullname" . }}
  labels:
    {{- include "python_practice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "python_practice.selectorLabels" . | nindent 4 }}
